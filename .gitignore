target/
project/target/
project/project/
.bsp/
.bloop/
.metals/
.scala-build/
*.log
.DS_Store
.idea/

# Locally installed skills, deliberately not shipped with the project. Vendored
# skills live in .agents/skills and are symlinked into .claude/skills; anything
# that is a real directory there was installed for local use only.
.claude/skills/humanizer/
.claude/skills/archify/

# Plugin enablement for a marketplace plugin that is not vendored here, so a
# fresh clone would enable something it does not have.
.claude/settings.json
